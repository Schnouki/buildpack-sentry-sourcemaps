#!/usr/bin/env bash
# bin/detect <build-dir>
set -e

build=$(cd "$1/" && pwd)

if test -n "$(find ${build}/${SOURCEMAP_DIR} -maxdepth 1 -type f -name '*.js.map' | sed 1q)"
then
    echo SentrySourcemap
    exit 0
else
    exit 1
fi
